---
'@kernhq/module-quire': patch
---

The page picker behind the "embed a page" block has its words: its title, search box, space
selector and empty state were rendering their message keys, in every language.
